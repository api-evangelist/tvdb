naftiko: 1.0.0-alpha2
info:
  label: TVDB API V4 — Series Statuses
  description: 'TVDB API V4 — Series Statuses. 1 operations. Lead operation: Get All Series Statuses. Self-contained Naftiko capability covering one TheTVDB business surface.'
  tags:
  - TheTVDB
  - Television
  - Series Statuses
  created: '2026-05-30'
  modified: '2026-05-30'
binds:
- namespace: env
  keys:
    TVDB_BEARER_TOKEN: TVDB_BEARER_TOKEN
capability:
  consumes:
  - type: http
    namespace: v4-series-statuses
    baseUri: 'https://api4.thetvdb.com/v4'
    description: TVDB API V4 — Series Statuses business capability. Self-contained, no shared references.
    authentication:
      type: bearer
      token: '{{env.TVDB_BEARER_TOKEN}}'
    resources:
    - name: series-statuses
      path: /series/statuses
      operations:
      - name: getAllSeriesStatuses
        method: GET
        description: TheTVDB Get All Series Statuses
        inputParameters: []
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: v4-series-statuses-rest
    port: 8080
    description: REST adapter for TVDB API V4 — Series Statuses. One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/series/statuses
      name: series-statuses
      description: REST surface for series-statuses.
      operations:
      - method: GET
        name: getAllSeriesStatuses
        description: TheTVDB Get All Series Statuses
        call: v4-series-statuses.getAllSeriesStatuses
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: v4-series-statuses-mcp
    port: 9090
    transport: http
    description: MCP adapter for TVDB API V4 — Series Statuses. One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: get-series-statuses
      description: TheTVDB Get All Series Statuses
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: v4-series-statuses.getAllSeriesStatuses
      outputParameters:
      - type: object
        mapping: $.
