naftiko: 1.0.0-alpha2
info:
  label: TVDB API V4 — Source Types
  description: 'TVDB API V4 — Source Types. 1 operations. Lead operation: Get All Source Types. Self-contained Naftiko capability covering one TheTVDB business surface.'
  tags:
  - TheTVDB
  - Television
  - Source Types
  created: '2026-05-30'
  modified: '2026-05-30'
binds:
- namespace: env
  keys:
    TVDB_BEARER_TOKEN: TVDB_BEARER_TOKEN
capability:
  consumes:
  - type: http
    namespace: v4-source-types
    baseUri: 'https://api4.thetvdb.com/v4'
    description: TVDB API V4 — Source Types business capability. Self-contained, no shared references.
    authentication:
      type: bearer
      token: '{{env.TVDB_BEARER_TOKEN}}'
    resources:
    - name: sources-types
      path: /sources/types
      operations:
      - name: getAllSourceTypes
        method: GET
        description: TheTVDB Get All Source Types
        inputParameters: []
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: v4-source-types-rest
    port: 8080
    description: REST adapter for TVDB API V4 — Source Types. One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/sources/types
      name: sources-types
      description: REST surface for sources-types.
      operations:
      - method: GET
        name: getAllSourceTypes
        description: TheTVDB Get All Source Types
        call: v4-source-types.getAllSourceTypes
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: v4-source-types-mcp
    port: 9090
    transport: http
    description: MCP adapter for TVDB API V4 — Source Types. One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: get-source-types
      description: TheTVDB Get All Source Types
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: v4-source-types.getAllSourceTypes
      outputParameters:
      - type: object
        mapping: $.
